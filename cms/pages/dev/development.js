import Section from "../../components/Section/Section";

const development = ({ meta, setMeta }) => {
  return (
    <Section flex center minHeight="100vh">
      <h2>Development</h2>
    </Section>
  );
};

export default development;
