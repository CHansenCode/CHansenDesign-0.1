import Section from "../../components/Section/Section";

const pages = ({ meta, setMeta }) => {
  return (
    <Section flex center minHeight="100vh">
      <h2>Statistics of page hits</h2>
    </Section>
  );
};

export default pages;
