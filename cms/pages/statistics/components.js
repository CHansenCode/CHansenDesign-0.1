import Section from "../../components/Section/Section";

const components = ({ meta, setMeta }) => {
  return (
    <Section flex center minHeight="100vh">
      <h2>Statistics of component usage on site</h2>
    </Section>
  );
};

export default components;
